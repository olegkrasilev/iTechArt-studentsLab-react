export type Posts = {
  user: string;
  title: string;
  post: string;
  postCreationTime: string;
  id: number;
};

export type Users = {
  email: string;
  firstName: string;
  lastName: string;
  id: number;
};
